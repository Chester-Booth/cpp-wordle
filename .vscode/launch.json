{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++: build and run",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/wordle",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "C/C++: g++ build"
        }
    ]
}
